spring:
  application:
    name: user-service
  config:
    import: "configserver:http://config-server:9000"

  cloud:
    config:
      username: ${CONFIG_SERVER_ID}
      password: ${CONFIG_SERVER_PASSWORD}


